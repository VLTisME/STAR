# Compatibility entry point for the modern PE/bbox stack.
#
# This file deliberately does NOT declare torch or torchvision. Install their matching CUDA
# wheels first, following docs/server_runbook.md. The legacy X-VLM stack MUST NOT install this
# file because it uses transformers==4.12.5 and timm==0.4.9 in a separate environment.

-r requirements-pe-a100.txt
